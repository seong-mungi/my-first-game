# Architecture Decision Registry
#
# PURPOSE: Single source of truth for key architectural stances that all ADRs
# must agree on. Skills check this before writing new ADRs to detect conflicts
# at authoring time — before stories are created against contradictory decisions.
#
# RULES:
#   - Register a stance when it constrains how OTHER systems must be built.
#     An internal implementation detail that affects only one ADR does not need
#     to be registered.
#   - Never delete entries — set status: superseded_by: ADR-NNNN instead.
#   - When a stance changes: update the entry, set revised: to today's date,
#     add a comment with the old value, and run /architecture-review to check
#     what ADRs are now invalidated.
#   - adr: is the authoritative source. All other ADRs that depend on this
#     stance list themselves in referenced_by.
#
# WRITTEN BY: /architecture-decision (Phase 5 — after ADR is approved)
# READ BY:    /architecture-decision (Step 2 — before authoring begins)
#             /architecture-review (Phase 4 — cross-ADR conflict baseline)
#             /create-stories (to embed architectural constraints in stories)
#             /dev-story (to check implementation against accepted stances)
#
# SEARCH PATTERNS (for skills using Grep):
#   All state owners:       Grep pattern="^  - state:" path="docs/registry/architecture.yaml"
#   Who owns player_health: Grep pattern="state: player_health"
#   All signal contracts:   Grep pattern="pattern: signal"
#   Budget for combat:      Grep pattern="system: combat"
#   All forbidden patterns: Grep pattern="^  - pattern:" (forbidden_patterns section)
#   Superseded entries:     Grep pattern="status: superseded"

version: 1
last_updated: "2026-05-11"  # Input System #1 Designed (input.md F.4.1 #13 obligation): 7 new entries — state_ownership.input_actions_catalog, interfaces.input_polling_contract, api_decisions.pause_handler_autoload + active_profile_tracker, forbidden_patterns.gameplay_input_in_callback + wall_clock_in_input_logic + deadzone_in_consumer. Previous (2026-05-10): Player Movement #6 Designed (player-movement.md F.4.1 #6 obligation): 4 new entries — state_ownership.player_movement_state, interfaces.player_movement_snapshot, forbidden_patterns.delta_accumulator_in_movement, api_decisions.facing_direction_encoding. Previous (2026-05-09): damage GDD Round 2 review (DEC-5 boss_hit_absorbed 2-arg + DEC-6 hazard_grace_frames + boss_pattern_interrupted signal + start_hazard_grace invocation_api + emit ordering contract).

# ─── STATE OWNERSHIP ─────────────────────────────────────────────────────────
# Who is the authoritative owner of each piece of shared game state.
# Conflicts: two ADRs claiming to own the same state = data race / authority bug.
#
# Register state when it is READ or WRITTEN by more than one system.
# Internal-only state (never accessed by another system) does not need to be registered.
#
# Required fields: state, owner_system, adr, interface, referenced_by[], added
# interface: how other systems access this state (read-only property, method call,
#   signal, shared resource, etc.) — this is the contract other ADRs depend on.

state_ownership:
  - state: player_rewind_state
    status: active
    owner_system: time-rewind-controller
    adr: docs/architecture/adr-0001-time-rewind-scope.md
    interface: "TimeRewindController.get_remaining_tokens() (read-only int) + signals (rewind_started, rewind_completed, token_consumed, token_replenished)"
    write_access: time-rewind-controller-only
    referenced_by:
      - docs/architecture/adr-0001-time-rewind-scope.md
      - docs/architecture/adr-0002-time-rewind-storage-format.md
    added: 2026-05-09
    revised: ""
    notes: "Ring buffer of last 90 frames of PlayerSnapshot resources. Only ECHO is rewound — enemies/bullets/environment continue normal simulation. See ADR-0001 Decision section. Storage format finalized in ADR-0002 (State Snapshot ring buffer, write-into-place pre-allocated)."

  - state: echo_lifecycle_state
    status: active
    owner_system: echo-lifecycle-sm  # EchoLifecycleSM (StateMachine instance, child of ECHO node)
    adr: design/gdd/state-machine.md  # GDD-owned (no ADR yet — framework-internal decision)
    interface: "EchoLifecycleSM.current_state.name (read-only StringName) + signal state_changed(from, to) + method should_swallow_pause() -> bool"
    write_access: echo-lifecycle-sm-only  # external systems must call transition_to(); direct current_state assignment is forbidden (see forbidden_patterns.direct_current_state_assignment)
    referenced_by:
      - design/gdd/state-machine.md
      - design/gdd/time-rewind.md  # Time Rewind GDD imposes 6 hosting obligations on this SM (C.2.2 of state-machine.md)
    added: 2026-05-09
    revised: ""
    notes: "4 states: ALIVE / DYING / REWINDING / DEAD. Behavior definition is single-sourced in design/gdd/time-rewind.md Section C; state-machine.md hosts only the framework obligations (latch, pause-swallow, signal subscriptions, scene clear). Pause system polls should_swallow_pause(); HUD/VFX/Audio subscribe to state_changed for visual/audio reactions."

  - state: boss_phase_state
    status: active
    owner_system: boss-pattern-system  # per-boss host owns phase_index + phase_hits_remaining
    adr: design/gdd/damage.md  # GDD-owned (boss host follows Damage GDD D.2 transition formula)
    interface: "Boss host members (read-only): phase_index: int, phase_hits_remaining: int, phase_hp_table: Array[int], final_phase_index: int. Mutation only via Damage system's HitBox→HurtBox path triggering D.2.1 transition. Signals: boss_hit_absorbed(boss_id, phase_index, hits_remaining), boss_phase_advanced(boss_id, new_phase), boss_killed(boss_id)."
    write_access: damage-system-mediated  # only Damage system's hurtbox_hit handler decrements; boss host validates table at _ready() (E.11 / AC-15)
    referenced_by:
      - design/gdd/damage.md
    added: 2026-05-09
    revised: ""
    notes: "Discrete phase thresholds (DEC-2). NO HP bar (Anti-Pillar Reveal Through Action). HUD subscribes to boss_phase_advanced only. Multi-hit same-frame transition is single-step bounded (D.2.3 — phase_index += 1 max even if remaining' goes negative). phase_hp_table values are owned per-boss by Boss Pattern GDD #11; Damage GDD owns the format contract (Array[int], length = final_phase_index + 1)."

  - state: player_movement_state
    status: active
    owner_system: player-movement  # PlayerMovement (CharacterBody2D, ECHO root, process_physics_priority=0)
    adr: design/gdd/player-movement.md
    interface: |
      Read (per-tick, 7 fields — PlayerSnapshot capture set; player-movement.md C.1.3):
        PlayerMovement.global_position: Vector2
        PlayerMovement.velocity: Vector2
        PlayerMovement.facing_direction: int           # 0..7 enum (api_decisions.facing_direction_encoding)
        PlayerMovement.current_animation_name: StringName
        PlayerMovement.current_animation_time: float
        PlayerMovement.current_weapon_id: int
        PlayerMovement.is_grounded: bool
      Write: PlayerMovement.restore_from_snapshot(snap: PlayerSnapshot) -> void
        — single legitimate restoration path during rewind (forbidden_patterns.direct_player_state_write_during_rewind enforces this).
    write_access: player-movement-only-or-snapshot-restore  # external systems read-only; only TRC may invoke restore_from_snapshot during a rewind operation
    referenced_by:
      - design/gdd/player-movement.md
      - design/gdd/time-rewind.md      # F.1 #6 — TRC reads 7 fields per tick + invokes restore_from_snapshot
      - design/gdd/state-machine.md    # F.2 row #6 — PlayerMovementSM hosted as sibling of EchoLifecycleSM under PlayerMovement
      - design/gdd/damage.md           # F.1 row #6 — ECHO HurtBox/HitBox/Damage hosted as PlayerMovement children
    added: 2026-05-10
    revised: ""
    notes: "PlayerMovement IS the ECHO root node itself (CharacterBody2D), not a child of an outer ECHO node — see player-movement.md A.Overview Decision A. PlayerMovementSM (extends StateMachine, 6 Tier 1 states: idle/run/jump/fall/aim_lock/dead per DEC-PM-1) is a sibling SM next to EchoLifecycleSM under PlayerMovement (Tier 1 flat composition, NOT parallel ownership — PlayerMovementSM's dead state is reactive to EchoLifecycleSM state_changed via T13). _is_restoring: bool guard (player-movement.md C.4.4 single source) blocks anim method-track callbacks (footstep SFX, dust VFX, bullet spawn) during restore. process_physics_priority=0 (ADR-0003 ladder). 7-field schema is the verbatim interface locked across player-movement.md C.1.3 + time-rewind.md F.1 #6 — any change requires both GDD updates + ADR-0002 Amendment."

  - state: input_actions_catalog
    status: active
    owner_system: input-system
    adr: design/gdd/input.md
    interface: |
      Read (per-tick polling via _physics_process Phase 2 ONLY — never _input/_unhandled_input):
        Input.is_action_pressed(InputActions.X: StringName) -> bool
        Input.is_action_just_pressed(InputActions.X: StringName) -> bool
        Input.is_action_just_released(InputActions.X: StringName) -> bool
        Input.get_vector(InputActions.MOVE_LEFT, MOVE_RIGHT, MOVE_UP, MOVE_DOWN) -> Vector2  # radial composite deadzone 0.2
        Input.get_action_strength(InputActions.X: StringName) -> float
      Write: only via InputMap dynamic mutation in paused tree (SettingsManager.apply_deadzone() / SettingsManager.apply_remap() Tier 3) — Tier 1/2 NO runtime mutation.
      Action catalog (9 fixed): move_left, move_right, move_up, move_down, jump, aim_lock, shoot, rewind_consume, pause.
      Action constants: src/input/input_actions.gd (StringName const class — `InputActions.JUMP = &"jump"` etc).
    write_access: input-system-only-via-settings-manager  # external systems read-only; runtime mutation only via SettingsManager in paused tree (Tier 3)
    referenced_by:
      - design/gdd/input.md
      - design/gdd/player-movement.md      # C.5.1 — PM polls 5 actions in _physics_process Phase 2
      - design/gdd/state-machine.md        # D.2 — SM polls rewind_consume per-tick at AliveState/DyingState physics_update
      - design/gdd/time-rewind.md          # C.3 #1 — TRC verifies SM input state
    added: 2026-05-11
    revised: ""
    notes: "9-action catalog locked Tier 1; Tier 2 zero additions (Difficulty Toggle #20 changes RewindPolicy data only, never InputMap); Tier 3 #23 Input Remapping permits player-bound chord (default mapping table unchanged — 'default_only' interpretation, OQ-15 closure). KB+M defaults: A/D/W/S move, Space jump, F aim_lock (NOT Shift — Shift conflict resolved to rewind_consume), LMB shoot, Shift rewind_consume, Escape pause. Gamepad defaults (Xbox labels; Steam Deck inherits verbatim): Left Stick move, A jump, RB aim_lock, RT shoot, LT(threshold 0.5) rewind_consume, Start pause. PauseHandler autoload (api_decisions.pause_handler_autoload) handles pause toggle via _unhandled_input on PROCESS_MODE_ALWAYS path. ActiveProfileTracker autoload (api_decisions.active_profile_tracker) tracks KB+M/GAMEPAD profile for first-death prompt label (D.1 formula, HYSTERESIS_FRAMES=180)."

# Example:
#
# state_ownership:
#   - state: player_health
#     status: active           # active | superseded_by: ADR-NNNN
#     owner_system: health-system
#     adr: docs/architecture/adr-0001-health-system.md
#     interface: "HealthComponent.current_health (read-only float, range 0–max_health)"
#     write_access: health-system-only   # only owner can write; others read
#     referenced_by:
#       - docs/architecture/adr-0001-health-system.md
#       - docs/architecture/adr-0003-combat-system.md   # reads to apply damage
#       - docs/architecture/adr-0007-ui-system.md       # reads to display HUD
#     added: 2026-03-26
#     revised: ""


# ─── INTERFACE CONTRACTS ──────────────────────────────────────────────────────
# How systems are required to communicate with each other.
# Conflicts: one ADR expects signal, another expects direct call = integration bug.
#
# Register a contract when the communication pattern between two systems is
# explicitly decided in an ADR and other systems must follow the same pattern.
#
# pattern options: signal | direct_call | event_bus | shared_resource | rpc | none

interfaces:
  - contract: rewind_lifecycle
    status: active
    pattern: signal
    producer: time-rewind-controller
    consumers:
      - hud-system          # subscribes to update REWIND token counter UI
      - vfx-system          # subscribes to trigger color-invert + glitch shader
      - audio-system        # subscribes to play rewind SFX cue
      - enemy-ai-base       # subscribes to optionally re-acquire ECHO target
      - boss-pattern-system # subscribes to handle boss-kill token replenish
      - state-machine       # subscribes to drive ALIVE / DYING / REWINDING / DEAD transitions
    adr: docs/architecture/adr-0001-time-rewind-scope.md
    signal_signature: |
      rewind_started(remaining_tokens: int)
      rewind_completed(player: Node2D, restored_to_frame: int)
      rewind_protection_ended(player: Node2D)  # added by design/gdd/time-rewind.md (REWINDING → ALIVE, 30 frames after rewind_started)
      token_consumed(remaining_tokens: int)
      token_replenished(new_total: int)
    referenced_by:
      - docs/architecture/adr-0001-time-rewind-scope.md
      - design/gdd/time-rewind.md  # extends contract with rewind_protection_ended (i-frame end signal)
    added: 2026-05-09
    revised: 2026-05-09

  - contract: state_lifecycle
    status: active
    pattern: signal
    producer: any-state-machine-instance  # every StateMachine node emits this — multiple producers across entities (ECHO, enemies, bosses)
    consumers:
      - vfx-system            # subscribes to trigger DYING pulse, REWINDING glitch shader
      - audio-system          # subscribes to play state-entry SFX cues
      - debug-overlay         # Tier 2 — displays "[ECHO] ALIVE → DYING (frame 12345)"
    adr: design/gdd/state-machine.md  # GDD-owned framework primitive
    signal_signature: |
      state_changed(from_state: StringName, to_state: StringName)
    referenced_by:
      - design/gdd/state-machine.md
    added: 2026-05-09
    revised: ""

  - contract: damage_signals
    status: active
    pattern: signal
    producer: damage-system  # ECHO Damage component + per-host Damage handlers (enemy/boss/hazard)
    consumers:
      - state-machine                # subscribes to player_hit_lethal (DYING transition)
      - time-rewind-controller       # subscribes to lethal_hit_detected (frame N cache) + death_committed (cleanup)
      - hud-system                   # subscribes to boss_phase_advanced (UI notification, NO HP bar)
      - vfx-system                   # subscribes to hurtbox_hit, boss_hit_absorbed, boss_phase_advanced
      - audio-system                 # subscribes to hurtbox_hit, enemy_killed, boss_phase_advanced, death_committed
      - boss-pattern-system          # subscribes to its own boss_phase_advanced (pattern SM re-entry)
      - scene-manager                # subscribes to boss_killed (stage clear trigger)
    adr: design/gdd/damage.md  # GDD-owned (8-signal catalog F.3)
    signal_signature: |
      lethal_hit_detected(cause: StringName)        # ECHO HurtBox area_entered, frame N
      player_hit_lethal(cause: StringName)          # same frame N, SM-targeted
      death_committed(cause: StringName)            # frame N+12, after grace expiry
      hurtbox_hit(cause: StringName)                # per-HurtBox instance signal (any entity)
      enemy_killed(enemy_id: StringName, cause: StringName)
      boss_hit_absorbed(boss_id: StringName, phase_index: int)             # DEC-5 (Round 2): hits_remaining REMOVED for binary contract protection
      boss_pattern_interrupted(boss_id: StringName, prev_phase_index: int) # NEW Round 2: phase advance/kill 직전 emit, Boss Pattern SM cleanup trigger
      boss_phase_advanced(boss_id: StringName, new_phase: int)
      boss_killed(boss_id: StringName)
    invocation_api: |
      Damage.commit_death() -> void                # called by SM DyingState.exit() on grace expiry; idempotent (no-op if _pending_cause empty)
      Damage.cancel_pending_death() -> void        # called by SM RewindingState.enter() on rewind consume; idempotent
      Damage.start_hazard_grace() -> void          # NEW Round 2 (DEC-6): called by SM RewindingState.exit(); 12-frame hazard-only grace
    emit_ordering_contract: |
      # F.4.1 — same-frame emit order (Pillar 2 determinism)
      ECHO hurt path: _pending_cause set → lethal_hit_detected → player_hit_lethal → hurtbox_hit
      Boss hurt path: hurtbox_hit → (boss_hit_absorbed | boss_pattern_interrupted → boss_phase_advanced | boss_pattern_interrupted → boss_killed)
      Multi-enemy same-frame death: enemy_killed emit order = scene-tree-order (ADR-0003 spawn orchestrator)
    referenced_by:
      - design/gdd/damage.md
      - design/gdd/time-rewind.md   # F.1 #8 row consumes lethal_hit_detected + death_committed
      - design/gdd/state-machine.md # F.1 #8 row consumes player_hit_lethal + invokes Damage methods + start_hazard_grace at RewindingState.exit()
    added: 2026-05-09
    revised: 2026-05-09  # Round 2 review: DEC-5 + DEC-6 + boss_pattern_interrupted + start_hazard_grace + emit ordering contract
    notes: "All cause-bearing signals use 1-arg cause: StringName (DEC-1, locked). cause taxonomy single-sourced in damage.md D.3 with hazard_ prefix invariant (DEC-6). 2-stage separation (lethal_hit_detected → grace → death_committed) is owned by Damage but timing controlled by SM DyingState (DEC-4 — Damage does NOT poll SM state). i-frame toggle is HurtBox.monitorable (NOT monitoring) per Godot 4.6 Area2D semantics. Boss phase advance is monotonic +1 (D.2.3 — perfect parry skip explicitly NOT supported, queued ADR boss-phase-advance-monotonicity)."

  - contract: player_movement_snapshot
    status: active
    pattern: shared_resource  # PlayerSnapshot Resource passed via method call (TRC ↔ PlayerMovement)
    producer: time-rewind-controller  # TRC reads PlayerMovement state per tick, writes to ring-buffered PlayerSnapshot Resources, then invokes restore_from_snapshot back into PlayerMovement during rewind
    consumers:
      - player-movement  # invoked back via restore_from_snapshot(snap) during rewind — single legitimate write path
    adr: design/gdd/player-movement.md
    method_signature: |
      PlayerMovement.restore_from_snapshot(snap: PlayerSnapshot) -> void
    snapshot_schema: |
      class_name PlayerSnapshot extends Resource:
        captured_at_physics_frame: int     # ADR-0002 schema field — set by TRC at capture
        global_position: Vector2
        velocity: Vector2
        facing_direction: int              # 0..7 enum (api_decisions.facing_direction_encoding)
        current_animation_name: StringName
        current_animation_time: float
        current_weapon_id: int
        is_grounded: bool
    referenced_by:
      - design/gdd/player-movement.md
      - design/gdd/time-rewind.md
      - docs/architecture/adr-0002-time-rewind-storage-format.md
    added: 2026-05-10
    revised: ""
    notes: "Single legitimate write path during rewind. restore_from_snapshot must (1) set _is_restoring = true before field assignments, (2) assign 6 numeric/StringName fields, (3) AnimationPlayer.play(snap.current_animation_name) + seek(snap.current_animation_time, true) — the second arg true is mandatory in Godot 4.6 (forces immediate evaluation, time-rewind.md I4), (4) clear _is_restoring at end. Anim method-track callbacks (footstep SFX, dust VFX, bullet spawn) MUST short-circuit on _is_restoring (player-movement.md C.4.4) to prevent spurious side effects during restore. forbidden_patterns.direct_player_state_write_during_rewind ban is enforced at PlayerMovement (single enforce site)."

  - contract: input_polling_contract
    status: active
    pattern: direct_call  # Input.is_action_* polled directly from consumers' _physics_process
    producer: input-system  # Godot Input singleton + InputMap
    consumers:
      - player-movement       # C.5.1 — 5 actions: 4 move + jump + aim_lock
      - state-machine         # D.2 — rewind_consume at AliveState/DyingState physics_update
      - time-rewind-controller # process_physics_priority=1 verify-only same-tick poll
      - player-shooting       # provisional — shoot action; detect mode (#7 decision)
      - pause-handler         # _unhandled_input on PROCESS_MODE_ALWAYS autoload — pause action exception
      - menu-pause-ui         # _input callback exception — UI/Menu actions only (NOT gameplay)
    adr: design/gdd/input.md
    polling_contract: |
      Cascade invariant: in a single _physics_process tick, ALL gameplay consumers MUST read the same InputMap action state. This invariant is the foundation of ADR-0003 determinism and SM 16-frame rewind_consume window (state-machine.md D.2).
      Rule 1: Input.is_action_*, Input.get_vector, Input.get_action_strength called ONLY in _physics_process.
      Rule 2: NO gameplay logic in _input / _unhandled_input / _unhandled_key_input. EXCEPTION: UI/Menu (#18) + PauseHandler autoload.
      Rule 3: Phase 2 (input poll) MUTATION-FREE — read only. State transitions and velocity calculations belong to Phase 3+ (player-movement.md C.3 5-phase pattern single source).
      Rule 4: GUT test injection: Input.action_press(InputActions.X) XOR Input.parse_input_event(InputEventAction). NEVER mix in same test fixture (E-IN-16). Helper files: tests/helpers/input_action_press_helper.gd OR tests/helpers/input_event_inject_helper.gd.
      Rule 5: action names use StringName constants from src/input/input_actions.gd (`InputActions.JUMP` NOT `&"jump"` inline).
    referenced_by:
      - design/gdd/input.md
      - design/gdd/player-movement.md
      - design/gdd/state-machine.md
      - design/gdd/time-rewind.md
      - docs/architecture/adr-0003-determinism-strategy.md
    added: 2026-05-11
    revised: ""
    notes: "Foundation/Infrastructure contract — Input doesn't own any gameplay state, but the cascade invariant it provides is load-bearing for every higher invariant (PM jump_buffer / SM rewind window / TRC snapshot capture). Violations are detectable via tools/ci/_input_purity.py (callback purity static check) + tools/ci/input_action_static_check.sh (StringName const usage). Both scripts are AC-IN-04/05 deliverables — meta-runner tools/ci/run_static_checks.sh asserts script existence (PM B2 vaccine)."

# Example:
#
# interfaces:
#   - contract: damage_delivery
#     status: active
#     pattern: signal          # how the producer notifies consumers
#     producer: combat-system
#     consumers:
#       - health-system        # receives damage_dealt signal, applies to health
#       - ui-system            # receives damage_dealt signal, shows damage number
#       - audio-system         # receives damage_dealt signal, plays SFX
#     adr: docs/architecture/adr-0003-combat-system.md
#     signal_signature: "damage_dealt(amount: float, target: Node, is_crit: bool)"
#     referenced_by:
#       - docs/architecture/adr-0003-combat-system.md
#       - docs/architecture/adr-0001-health-system.md
#     added: 2026-03-26
#     revised: ""


# ─── PERFORMANCE BUDGETS ──────────────────────────────────────────────────────
# Frame time allocations per system, against a defined total frame budget.
# Conflicts: allocations that sum to more than the total budget = unshippable.
#
# Register a budget when an ADR explicitly claims a frame time allocation.
# The registry lets /architecture-review verify the total doesn't exceed the target.

performance_budgets: []

# Example:
#
# performance_budgets:
#   - target_fps: 60
#     total_frame_budget_ms: 16.6
#     platform: PC             # PC | Console | Mobile | All
#     adr: docs/architecture/adr-0000-performance-targets.md
#     added: 2026-03-26
#     revised: ""
#
#   - system: combat
#     status: active
#     budget_ms: 2.0
#     adr: docs/architecture/adr-0003-combat-system.md
#     notes: "Includes physics queries, hitbox evaluation, and damage calculation"
#     referenced_by:
#       - docs/architecture/adr-0003-combat-system.md
#     added: 2026-03-26
#     revised: ""


# ─── API DECISIONS ───────────────────────────────────────────────────────────
# Which engine APIs are chosen for which purpose across the project.
# Conflicts: two ADRs using different APIs for the same purpose = inconsistent
# implementation that breaks when one system expects the other's API.
#
# Register an API decision when the choice is non-obvious and other ADRs might
# make a different choice for the same purpose without knowing this was decided.

api_decisions:
  - purpose: time_rewind_scope
    status: active
    api: "Player-only checkpoint model — ECHO state ring buffer (90 frames @ 60fps)"
    not: "Braid model (all dynamic entities) — explicitly rejected for Pillar 2 + solo budget + story integrity"
    adr: docs/architecture/adr-0001-time-rewind-scope.md
    reason: "ECHO-only rewind preserves deterministic enemy patterns (Pillar 2), aligns with REWIND Core personal-device fiction (Story Spine), and fits solo 4-6w Tier 1 budget (Pillar 5)."
    referenced_by:
      - docs/architecture/adr-0001-time-rewind-scope.md
    added: 2026-05-09
    revised: ""

  - purpose: time_rewind_storage
    status: active
    api: "State Snapshot ring buffer — pre-allocated 90 PlayerSnapshot Resources, write-into-place, no per-tick allocation"
    not: "Input Replay (rejected — Godot 4.6 2D physics determinism unguaranteed; Pillar 2 risk). Hybrid (rejected — solo budget waste; 1000-cycle automated test sufficient)."
    adr: docs/architecture/adr-0002-time-rewind-storage-format.md
    reason: "External-dependency-zero data restoration guarantees 100% determinism (Pillar 2 verifiable). 5KB memory budget (0.0003% of 1.5GB ceiling). Save system reuses same Resource serialization. ~90 lines GDScript estimated."
    referenced_by:
      - docs/architecture/adr-0002-time-rewind-storage-format.md
      - docs/architecture/adr-0003-determinism-strategy.md
    added: 2026-05-09
    revised: ""

  - purpose: physics_node_strategy
    status: active
    api: "CharacterBody2D for ECHO + all enemies; Area2D for all projectiles; RigidBody2D cosmetic-only (debris/gibs on layer disjoint from gameplay)"
    not: "RigidBody2D unified (rejected — solver float drift breaks Pillar 2 + ADR-0002 snapshot contract). Mixed Player CharacterBody2D / Enemies+Projectiles RigidBody2D (rejected — Pillar 2 still broken for entities where it matters most)."
    adr: docs/architecture/adr-0003-determinism-strategy.md
    reason: "CharacterBody2D has no solver overriding directly-written transform — restore_from_snapshot() field assignment IS the next-tick state. Area2D has no integrator and never sleeps — projectile motion is fully script-owned. Single paradigm = solo-budget-friendly debug/profile surface. Verified via godot-specialist 2026-05-09."
    referenced_by:
      - docs/architecture/adr-0003-determinism-strategy.md
    added: 2026-05-09
    revised: ""

  - purpose: determinism_clock
    status: active
    api: "Engine.get_physics_frames() — monotonic int incremented each physics step; sole source of game time for AI/timer logic"
    not: "Time.get_ticks_msec() / wall-clock APIs (banned in gameplay-critical paths). Global RNG randi()/randf() (banned — use RandomNumberGenerator with explicit per-encounter seed)."
    adr: docs/architecture/adr-0003-determinism-strategy.md
    reason: "Wall-clock-derived behavior breaks 1000-cycle determinism test by construction. Frame-counter-driven AI is reproducible single-machine. Per-encounter seeded RNG keeps any randomness deterministic across player retries (Pillar 2)."
    referenced_by:
      - docs/architecture/adr-0003-determinism-strategy.md
    added: 2026-05-09
    revised: ""

  - purpose: physics_step_ordering
    status: active
    api: "Node.process_physics_priority ladder — player=0, time-rewind-controller=1, enemies=10, projectiles=20 (lower priority steps first); spawn orchestrators MUST insert children sorted by (spawn_frame, spawn_id)"
    not: "Node.process_priority (only orders _process(), NOT _physics_process — silent determinism break if used here). Implicit scene-tree-order fallback (unstable under runtime add_child/remove_child)."
    adr: docs/architecture/adr-0003-determinism-strategy.md
    reason: "process_physics_priority is the explicit physics-step-ordering knob in 4.6. Without it, sibling order falls back to scene-tree order which is stable only if the tree is never mutated at runtime. Determinism boundary requires explicit ordering for the 1000-cycle test to pass under runtime spawn/despawn (validation criterion #6). TRC=1 ensures snapshot capture reads PlayerMovement after PlayerMovement._physics_process completes (avoids stale-read; design/gdd/time-rewind.md Integration Risk I8)."
    referenced_by:
      - docs/architecture/adr-0003-determinism-strategy.md
      - design/gdd/time-rewind.md  # adds time-rewind-controller=1 slot to the ladder
      - design/gdd/state-machine.md  # explicitly declares SM has NO slot in the ladder (C.3.1) — SM transitions inherit host priority via signal-reactive + tick-driven dispatch (C.3.2)
    added: 2026-05-09
    revised: 2026-05-09

  - purpose: collision_layer_assignment
    status: active
    api: |
      6-bit Area2D collision_layer/mask matrix (Damage GDD C.2):
        bit 1 = echo_hurtbox            (host: ECHO #6)
        bit 2 = echo_projectile_hitbox  (host: Player Shooting #7)
        bit 3 = enemy_hurtbox           (host: Enemy AI #10)
        bit 4 = enemy_projectile_hitbox (host: Enemy AI #10 + Boss Pattern #11)
        bit 5 = hazard_hitbox           (host: Stage #12)
        bit 6 = boss_hurtbox            (host: Boss Pattern #11)
      Mask matrix: ECHO HurtBox(L1) masks {L4,L5}; ECHO Projectile HitBox(L2) masks {L3,L6};
        Enemy HurtBox(L3) masks {L2}; Enemy Projectile HitBox(L4) masks {L1};
        Hazard HitBox(L5) masks {L1}; Boss HurtBox(L6) masks {L2}.
    not: "Ad-hoc per-system layer allocation (would create silent collision graph conflicts). PhysicsServer2D direct queries (gameplay path uses Area2D signals only)."
    adr: design/gdd/damage.md  # GDD-owned (D.1.1 / C.2)
    reason: "Centralizing layer assignment in Damage GDD prevents two systems claiming the same bit and provides single source for self-harm prevention (L2 mask omits L1) + friendly-fire blocking (L4 mask omits L3). All 5 host systems #6/#7/#10/#11/#12 must read this matrix; changes require Damage GDD update + ADR. Bits 7+ reserved for Tier 2 expansion (Pickup #15)."
    referenced_by:
      - design/gdd/damage.md
    added: 2026-05-09
    revised: ""

  - purpose: facing_direction_encoding
    status: active
    api: |
      facing_direction: int (0..7 enum, 8-way rose, CCW from East — verbatim from
      player-movement.md D.4 line 670):
        0 = E   (East)
        1 = NE  (Northeast)
        2 = N   (North)
        3 = NW  (Northwest)
        4 = W   (West)
        5 = SW  (Southwest)
        6 = S   (South)
        7 = SE  (Southeast)
      Internal -1 sentinel ("preserve previous") used only inside PM private helpers
      _encode_facing(v: Vector2i) / _decode_facing(f: int) (D.4 Formula 1) — never
      serialized to PlayerSnapshot. The null-input preservation rule (D.4 Formula 4)
      guarantees facing_direction ∈ {0..7} at every snapshot capture point.
    not: "Vector2 unit (would expand snapshot from 7 fields to 8 + serialization cost). float angle radians (rejected — float comparison fragility breaks animation branch determinism + requires conversion at every read site). enum/StringName (rejected — wider memory than int with no clarity advantage)."
    adr: design/gdd/player-movement.md
    reason: "int 0..7 encodes 8-way aim rose with zero schema growth — PlayerSnapshot stays at 7 fields (ADR-0002 unchanged, Decision B 2026-05-10). Bit-exact comparable across runs (Pillar 2 determinism). Animation lookup is a 1:8 dispatch (Sprite2D flip_h vs 8-anim branch — visual specifics deferred to player-movement.md C.6 + art-director consult, character movement category). _encode_facing/_decode_facing are PM private (D.4 Formula 1) — no external caller decodes the int directly except for animation/sprite branch and forbidden-pattern static analysis."
    referenced_by:
      - design/gdd/player-movement.md  # D.4 single source for encoding
      - docs/architecture/adr-0002-time-rewind-storage-format.md  # 7-field snapshot schema unchanged
    added: 2026-05-10
    revised: ""

  - purpose: pause_handler_autoload
    status: active
    api: "PauseHandler autoload (process_mode = PROCESS_MODE_ALWAYS) using _unhandled_input — single source for `pause` action toggle. Three decision paths: (a) resume — always allowed (no SM veto); (b) initiate — queries SM `can_pause()`, applies pause if true; (c) veto — SM returned false, pause swallowed silently. Calls get_viewport().set_input_as_handled() on **all three paths** (consume-on-decision invariant per AC-IN-20-A)."
    not: "Single SM polling of `pause` action in _physics_process (would deadlock — SM frame counter freezes when get_tree().paused = true; resume becomes impossible). Multiple uncoordinated _input listeners (introduces dispatch ordering ambiguity). Veto-without-consumption (would let event leak to other _unhandled_input listeners as if pause was never pressed; was earlier-draft pattern fixed by B4 reconciliation 2026-05-11)."
    adr: design/gdd/input.md
    reason: "Godot 4.6 canonical pattern for pause-toggle nodes. PROCESS_MODE_ALWAYS keeps the autoload responsive during pause. _unhandled_input (vs _input) lets UI/Menu (#18) consume directional/confirm inputs higher in the dispatch stack without blocking pause. set_input_as_handled() on every decision path (Godot 'consume on decision' pattern) prevents downstream _unhandled_input listeners from reinterpreting an already-resolved pause event. ActiveProfileTracker._input fires *before* _unhandled_input regardless of consumption (E-IN-NEW corrected ordering 2026-05-11) — consumption here doesn't block profile classification. gdscript-specialist Q5 confidence: verified."
    referenced_by:
      - design/gdd/input.md            # C.1.4 single source
      - design/gdd/state-machine.md    # C.2.2 O2 — SM provides can_pause() query interface
    added: 2026-05-11
    revised: "2026-05-11 — B4 reconciliation: documented veto-path consumption explicitly (was implicit in earlier draft, contradicted AC-IN-20-A). Reason added for E-IN-NEW dispatch-order correction (Session 13 review B5)."

  - purpose: active_profile_tracker
    status: active
    api: "ActiveProfileTracker autoload (process_mode = PROCESS_MODE_ALWAYS) using _input — tracks last input source (KB+M vs GAMEPAD) for first-death prompt button-label disambiguation. Two-part API: (A) event-driven source classification updates `_last_input_source: DeviceProfile` + `_last_input_frame: int` on InputEventKey/MouseButton/MouseMotion → KB_M; InputEventJoypadButton → GAMEPAD; InputEventJoypadMotion → GAMEPAD if abs(axis_value) > GAMEPAD_DETECTION_THRESHOLD (0.3). (B) per-query resolution: `active_profile() -> DeviceProfile` returns GAMEPAD if last source was GAMEPAD within HYSTERESIS_FRAMES (180) else KB_M sticky else fallback (GAMEPAD if joypads_connected > 0 else KB_M). `button_label(profile, action_id) -> String` provides device-specific label."
    not: "Native Godot `Input.last_input_source` API (does not exist in Godot 4.6 — must implement). Symmetric KB_M/GAMEPAD hysteresis (rejected — KB_M sticky immediate is intentional: a mouse brush should not flip the gamepad-mode label; KB+M users actively press keys, gamepad users sometimes brush mouse)."
    adr: design/gdd/input.md
    reason: "First-death prompt (C.1.5) requires per-device label string (`[Shift] Rewind` vs `[LT] Rewind`). Without active profile heuristic, the prompt would have to guess or display both — Pillar 4 5분-룰 violation. HYSTERESIS_FRAMES=180 (3초) balances mouse-jitter false-flip prevention vs device-swap responsiveness; OQ-IN-3 pending Tier 1 physical Steam Deck 1세대 verification of GAMEPAD_DETECTION_THRESHOLD=0.3 against RMA stick drift range 0.05–0.18."
    referenced_by:
      - design/gdd/input.md            # D.1 single source + C.1.5
      - design/gdd/state-machine.md    # first_death_in_session signal owner (new — input.md F.3 O-IN-3 obligation)
      - design/gdd/time-rewind.md      # T1 DYING grace window aligns with prompt display duration
    added: 2026-05-11
    revised: ""

# Example:
#
# api_decisions:
#   - purpose: physics_raycast
#     status: active
#     api: PhysicsServer3D.space_get_direct_state()
#     not: RayCast3D node     # explicitly banned for this purpose
#     adr: docs/architecture/adr-0002-physics-architecture.md
#     reason: "Direct server calls avoid per-frame node overhead; 3× faster in profiling"
#     referenced_by:
#       - docs/architecture/adr-0002-physics-architecture.md
#       - docs/architecture/adr-0003-combat-system.md
#     added: 2026-03-26
#     revised: ""


# ─── FORBIDDEN PATTERNS ──────────────────────────────────────────────────────
# Architecture anti-patterns explicitly banned by accepted ADRs.
# When writing a new ADR, check this list — if the proposed approach matches
# a forbidden pattern, it must be reworked before the ADR can be Accepted.
#
# Register a pattern when an ADR explicitly bans it AND other ADRs might
# unknowingly use it (i.e., it's a tempting but wrong approach for this project).

forbidden_patterns:
  - pattern: direct_player_state_write_during_rewind
    status: active
    description: "External systems must NOT directly write to PlayerMovement.global_position, .velocity, .facing_direction, or animation state during a rewind operation. The single legitimate restoration path is PlayerMovement.restore_from_snapshot(snap: PlayerSnapshot)."
    why: "Direct state writes bypass the snapshot validation pipeline and create silent state-divergence bugs. The Snapshot ring buffer is the source of truth during rewind — multiple writers break determinism (Pillar 2)."
    adr: docs/architecture/adr-0002-time-rewind-storage-format.md
    referenced_by:
      - docs/architecture/adr-0002-time-rewind-storage-format.md
      - docs/architecture/adr-0003-determinism-strategy.md
    added: 2026-05-09

  - pattern: rigidbody2d_for_gameplay_entities
    status: active
    description: "ECHO, all enemies (drone/security bot/STRIDER boss/future archetypes), and all projectiles (player + enemy bullets, grenades, missiles) MUST NOT be RigidBody2D. Required types: ECHO + enemies = CharacterBody2D; projectiles = Area2D. RigidBody2D is permitted ONLY for cosmetic-only debris that (1) is on a collision layer disjoint from all gameplay layers, and (2) is destroyed before it could affect a snapshot capture cycle."
    why: "Godot Physics 2D solver introduces float drift not bit-identical across runs/machines, breaking Pillar 2 (deterministic patterns). Solver-driven motion also violates ADR-0002's snapshot contract: after PhysicsServer2D.body_set_state() the solver may re-settle within the same frame, changing actual restored state from what the snapshot recorded. Bullet trajectory non-determinism (tunneling/bouncing) under RigidBody2D additionally breaks reproducibility."
    adr: docs/architecture/adr-0003-determinism-strategy.md
    referenced_by:
      - docs/architecture/adr-0003-determinism-strategy.md
    added: 2026-05-09

  - pattern: direct_current_state_assignment
    status: active
    description: "External code MUST NOT directly assign StateMachine.current_state = some_state. The single legitimate path to change state is StateMachine.transition_to(new_state, payload, force_re_enter). Direct assignment bypasses exit/enter lifecycle, _is_transitioning lock, state_changed signal emission, and the transition queue (Q_cap)."
    why: "Direct assignment is silently broken — no signal emit means VFX/Audio/debug-overlay observers miss the transition; no exit() call means resource cleanup (timers, animations) leaks; no _is_transitioning lock means re-entrancy guard is bypassed. The bug surfaces only as visual/audio drift, hard to trace. design/gdd/state-machine.md AC-21 enforces this as a negative test (assignment must produce zero state_changed emits, proving the misuse is detectable)."
    adr: design/gdd/state-machine.md
    referenced_by:
      - design/gdd/state-machine.md
    added: 2026-05-09

  - pattern: state_machine_in_autoload
    status: active
    description: "StateMachine instances MUST NOT be Autoload Singletons. They must be child nodes of the host entity (ECHO, enemy, boss). Single-instance Autoload is incompatible with per-entity instantiation and breaks scene-reload lifecycle alignment."
    why: "Autoload persists across scene changes; SM ephemeral state (latch, frame counters, _is_transitioning) becomes stale on scene reload. Furthermore, multi-entity reuse (Tier 1 M1/M2/M3 = ECHO + drone + boss) requires per-host instantiation — Autoload singleton enforcement contradicts the Reuse Across Entities goal (state-machine.md C.4.1)."
    adr: design/gdd/state-machine.md
    referenced_by:
      - design/gdd/state-machine.md
    added: 2026-05-09

  - pattern: cross_entity_sm_transition_call
    status: active
    description: "An entity's StateMachine MUST NOT call StateMachine.transition_to() on another entity's StateMachine. For example, an enemy's SM must NOT directly call ECHO's EchoLifecycleSM.transition_to(DyingState). Cross-entity state changes must be communicated via signals — the signal recipient's own SM decides whether to transition."
    why: "Direct cross-entity calls create strong coupling that breaks 1000-cycle determinism (call timing depends on scene-tree traversal order, which can shift if entity spawn order changes). Signal-mediated communication preserves the connect-order determinism guaranteed by C.3.4 of state-machine.md. Also: cross-entity calls let bugs in one entity's SM corrupt another entity's state — debugging surface explodes."
    adr: design/gdd/state-machine.md
    referenced_by:
      - design/gdd/state-machine.md
    added: 2026-05-09

  - pattern: damage_polls_sm_state
    status: active
    description: "Damage system code MUST NOT read EchoLifecycleSM (or any StateMachine) state. This includes: state_machine.current_state, state_machine.is_rewinding(), state_machine.get_state(), or accessing the SM via NodePath. The i-frame mechanism is unidirectional: SM writes echo_hurtbox.monitoring; Damage observes that area_entered does not fire — no SM-aware code path in Damage."
    why: "Bidirectional Damage↔SM coupling breaks single-source-of-truth (DEC-4). If Damage polls SM state, two outcomes are possible: (1) SM transition not yet complete when Damage reads (race), (2) Damage's i-frame logic diverges from SM's monitoring toggle. Both produce silent invariant violations. AC-21 enforces this as a static-analysis grep ban. Verified by damage.md C.6.1 + D.4.3 single-source contract."
    adr: design/gdd/damage.md
    referenced_by:
      - design/gdd/damage.md
    added: 2026-05-09

  - pattern: delta_accumulator_in_movement
    status: active
    description: "PlayerMovement physics MUST NOT accumulate frame deltas into long-lived state that survives across _physics_process ticks without being reset (e.g., total_dt: float, run_time_elapsed: float, gravity_velocity_acc: float). All movement integration uses per-tick velocity/position math derived from frame-bounded inputs: run accel/decel windowed by input edges (player-movement.md D.1), jump impulse one-shot (D.2), gravity applied per-tick to velocity directly (D.2). Coyote-time and jump-buffer windows count physics frames via Engine.get_physics_frames() with INT_MIN sentinel reset (D.3), NOT accumulated dt. The only PM long-lived state surviving across ticks is the 7-field PlayerSnapshot schema + a small set of frame-counter sentinels (_last_grounded_frame, _jump_buffered_at_frame) which are explicit int frames, not float accumulators."
    why: "Accumulators violate snapshot determinism — PlayerSnapshot only captures 7 fields, so any accumulated float state is invisible to the rewind contract and would silently desync after restore_from_snapshot. Accumulated float state also creates drift across long sessions, breaking 1000-cycle determinism (Pillar 2). Phase 4 unbounded-accumulation bug fix (player-movement.md C.3.1 post-write fix) drove this rule to architecture-registry-level so future PM changes are checked against this stance at authoring time, not discovered at the next cross-GDD review."
    adr: design/gdd/player-movement.md
    referenced_by:
      - design/gdd/player-movement.md
    added: 2026-05-10

  - pattern: gameplay_input_in_callback
    status: active
    description: "Gameplay systems (PM #6, SM #5, TRC #9, Player Shooting #7) MUST NOT poll Input.is_action_*, Input.get_vector, or Input.get_action_strength from _input / _unhandled_input / _unhandled_key_input callbacks. All gameplay input polling occurs in _physics_process Phase 2 only. EXCEPTIONS: UI/Menu system (#18) may use _input for directional/confirm actions; PauseHandler autoload (api_decisions.pause_handler_autoload) uses _unhandled_input for `pause` toggle."
    why: "Callback timing is decoupled from physics tick boundaries — same logical input arriving via _input may be processed in different physics frames than the same input polled in _physics_process. Cascade invariant (interfaces.input_polling_contract): all consumers must read the same action state in the same tick. Callbacks break that invariant. Wall-clock-style latency creep + cross-consumer disagreement = Pillar 2 determinism break. Detection: tools/ci/_input_purity.py runs Python multiline regex against PM/SM/TRC/PS source files; awk-range patterns are forbidden (PM B4 trap — collapses on single-line `func _input(e): pass`)."
    adr: design/gdd/input.md
    referenced_by:
      - design/gdd/input.md            # C.1.2 Rule 2 + AC-IN-05
      - design/gdd/player-movement.md  # C.6 row 470 cross-ref
    added: 2026-05-11

  - pattern: wall_clock_in_input_logic
    status: active
    description: "Input system code and input-driven gameplay logic MUST NOT call Time.get_ticks_msec(), Time.get_ticks_usec(), OS.get_ticks_msec(), or use InputEvent.timestamp (wall-clock milliseconds). All frame references and timing windows use Engine.get_physics_frames() (monotonic int) as the single clock source. Applies to: PM (jump_buffer / coyote_frames), SM (rewind_consume window), TRC (snapshot capture), Input (ActiveProfileTracker HYSTERESIS_FRAMES)."
    why: "ADR-0003 api_decisions.determinism_clock locks Engine.get_physics_frames() as the sole game-time source. Wall-clock-derived behavior breaks 1000-cycle determinism by construction (wall-clock drift across runs/machines). InputEvent.timestamp is a wall-clock value emitted by the OS — using it in gameplay logic introduces non-deterministic timing windows. Detection: tools/ci/wall_clock_check.sh grep gate. Banned only in gameplay-critical paths — UI animations / settings menus may use wall-clock for cosmetic timing."
    adr: design/gdd/input.md
    referenced_by:
      - design/gdd/input.md            # D.2 + AC-IN-18
      - docs/architecture/adr-0003-determinism-strategy.md  # determinism_clock single source
    added: 2026-05-11

  - pattern: deadzone_in_consumer
    status: active
    description: "Consumer gameplay systems (PM #6, SM #5, TRC #9, Player Shooting #7) MUST NOT re-implement deadzone math. Patterns like `if abs(Input.get_joy_axis(...)) > 0.2:` or `if vec.length() > 0.2:` against raw axis values are banned. Single enforcement site: InputMap action deadzone set in project.godot to 0.2 (radial composite, applied automatically by Input.get_vector). Runtime mutation only via SettingsManager.apply_deadzone() in paused tree (Tier 3 accessibility)."
    why: "Per-consumer deadzone re-implementation creates threshold drift: if one consumer sets 0.2 and another 0.25, the cascade invariant breaks. Also bypasses the radial composite semantics — Input.get_vector applies deadzone to magnitude, not per-axis; consumer-side per-axis checks produce different behavior than Input layer. PM B10 BLOCKING surfaced a related problem: `facing_threshold_outside` was set to the same value as `gamepad_deadzone` (both 0.2) creating no-hysteresis facing flicker on Steam Deck stick drift — that PM-specific issue is resolved by PM-side asymmetric thresholds (enter=0.2, exit=0.15), NOT by changing the Input deadzone. Detection: tools/ci/deadzone_consumer_check.sh grep gate."
    adr: design/gdd/input.md
    referenced_by:
      - design/gdd/input.md            # C.1.2 Rule 5 + C.1.3 + AC-IN-08
      - design/gdd/player-movement.md  # E-PM-9 + PM B10 cross-ref
    added: 2026-05-11

# Example:
#
# forbidden_patterns:
#   - pattern: autoload_singleton_coupling
#     status: active
#     description: "Systems must not directly reference Autoload singletons by name.
#       Use dependency injection or signals instead."
#     why: "Tight coupling to Autoloads makes systems untestable in isolation and
#       creates hidden dependencies that break when Autoload load order changes."
#     adr: docs/architecture/adr-0001-health-system.md
#     added: 2026-03-26
#
#   - pattern: direct_cross_system_state_write
#     status: active
#     description: "A system must never write to state owned by another system.
#       Use signals or method calls on the owner instead."
#     why: "Direct state writes create race conditions and break the state
#       ownership model. The owning system must be the sole writer."
#     adr: docs/architecture/adr-0000-architecture-principles.md
#     added: 2026-03-26
