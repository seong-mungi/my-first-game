# `src/` Working Notes

- This directory contains game source code, not skill or workflow definitions.
- Before changing code, confirm that the relevant design and architecture constraints are documented in `design/` or `docs/`.
- If a change is led by a specific studio role, read the corresponding `.claude/agents/*.md` file first.
